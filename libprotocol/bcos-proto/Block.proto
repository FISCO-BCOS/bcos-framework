syntax = "proto3";
package bcos.protocol;
import "BlockHeader.proto";

// Mainly used for block data packet encoding when network transmission
message Block{
    int32 version = 1;
    PBRawBlockHeader header = 2;
    repeated bytes transactions = 3;
    repeated bytes receipts = 4;
}
